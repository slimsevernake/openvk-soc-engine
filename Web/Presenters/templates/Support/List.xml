{extends "../@listView.xml"}

{block title}
    Helpdesk
{/block}

{block header}
    Helpdesk » {_support_tickets}
{/block}

{block tabs}
    <div n:attr="id => ($act === 'open' ? 'activetabs' : 'ki')" class="tab">
        <a n:attr="id => ($act === 'open' ? 'act_tab_a' : 'ki')" href="?">{_ticket_status_open_p}</a>
    </div>
    <div n:attr="id => ($act === 'answered' ? 'activetabs' : 'ki')" class="tab">
        <a n:attr="id => ($act === 'answered' ? 'act_tab_a' : 'ki')" href="?act=answered">{_ticket_status_answered_p}</a>
    </div>
    <div n:attr="id => ($act === 'closed' ? 'activetabs' : 'ki')" class="tab">
        <a n:attr="id => ($act === 'closed' ? 'act_tab_a' : 'ki')" href="?act=closed">{_ticket_status_closed_p}</a>
    </div>
{/block}

{* BEGIN ELEMENTS DESCRIPTION *}

{block link|strip|stripHtml}
    /support/reply/{$x->getId()}
{/block}

{block preview}
    <center>
        <img src="/assets/packages/static/openvk/img/note_icon.png" alt="Тикет" style="margin-top: 8px;">
    </center>
{/block}

{block name}
    {$x->getName()}
{/block}

{block description}
    {var author = $x->getUser()}
    
    {ovk_proc_strtr($x->getContext(), 50)}<br/>
    <span class="nobold">{_author}: </span> <a href="{$author->getURL()}">{$author->getCanonicalName()}</a>
{/block}
